# Session context — RideNDrive / RouteShare (saved 13 Jul 2026)

Working notes so any future session (or collaborator) can resume without re-deriving state.
Safe to delete once the project is submitted. **Do not commit** (add to .gitignore if desired).

## Project identity
- RideNDrive (repo folder: RouteShare) — commuter carpooling platform, SE course project.
- Student: Mohammad Haroon (560824), Prof. Salvatore Distefano, UniME.
- GitHub: https://github.com/SupraflyX/RideNDrive (push from PowerShell only).
- Stack: Spring Boot 3.2.5 / Java 17 / Hibernate 6.4.4; MariaDB via XAMPP (default), MySQL 8 Docker, H2 for `test` and `local` profiles. Frontend: vanilla SPA (index.html/css/js, cache `v=6`).

## Report (RideNDrive_Report_v2.docx)
- Generated by `report_gen.js` (Node + docx lib) in the session outputs dir; images from `outputs/word_safe/` (all 42 re-encoded Word-safe PNGs).
- Current build: 101 LibreOffice pages (≈110 in Word after TOC field update), 42 figures, 41 tables.
- Section 0 (review-response) REMOVED; Acknowledgements REMOVED; abstract neutral. Table numbering starts at "Agile methods".
- IMG() helper caps image height at 620 pt so tall figures stay on one page.
- LATEST BUILD NOT YET COPIED into this folder (Word had the file locked). The docx here may be one build behind: it still says "126 tests" — must be replaced.
- In Word: update fields on open (TOC/page numbers), then export PDF.

## Corruption incidents (disk failure aftermath)
Second corruption wave found 13 Jul and repaired from the session transcript:
- PricingEngine.java — tail rebuilt (SAME_DESTINATION + LOYALTY cases, final fare). Audit labels are exact: `DriverRule:LATE_NIGHT_FEE(+%.2f€)`, `DriverRule:SAME_DESTINATION_DISCOUNT(%.0f%%)`, `DriverRule:LOYALTY_TIER_DISCOUNT(%.0f%% for %s)`.
- BookingLifecycleService.java — tail rebuilt: rateableCounterparts driver loop, transition(), requireDriverActor() (403 helper), notifyPassenger/notifyDriver.
- BookingController.java — tail rebuilt incl. catch order SecurityException→403, IllegalStateException→409, RuntimeException→404, then @FunctionalInterface TransitionAction.
- RideRequest.java — trailing NULs stripped.
- GoogleMapsMappingService.java — `HttpResponse<String>` fix (decompiled raw type had crept back).
- Diagrams 1 (Layered Architecture) and 13 (Pricing Activity) were blank in PNG **and** PDF; rebuilt as graphviz (`report_artifacts/1_...gv`, `13_...gv`) and re-exported to diagram_pdfs (png+pdf).
- Full scan now clean: 112 text files, 42 images, docx CRC OK.

## Test suite — TRUE COUNT IS 121 (not 126)
- 121 @Test methods across: BookingLifecycle (18), IntegrationCoverage (61), PaymentAndRoute (6), PolicyEngine (20), RouteShareApplicationTests (1), unit tests 3×5. (Earlier "126" was never verified; all docs now say 121.)
- Docs synced to 121: README.md, REBUTTAL_EMAIL.md, RECOVERY_NOTES.md, report_gen.js (14 spots), CI pipeline diagram (18_...gv re-rendered).
- Last run: 121 run, 2 failures — BOTH FIXED, AWAITING RE-RUN:
  1. transitionOnMissingBooking 500→404: missing RuntimeException catch restored in BookingController.
  2. accountDeletion_cascadesOverLedgerRows: TripOffer.passengers has cascade=ALL + EAGER, so managed trips resurrect removed bookings at flush → FK violation on users. Fixed in UserService.delete(): detach booking from trip.getPassengers() before deleteAll.
- PolicyEngineIntegrationTest carries `@TestPropertySource(properties = "google.maps.api-key=")` — must be preserved (isolates tests from the real key).

## Product features (all implemented, this cycle)
Driver-owned policy engine (travel + pricing rules, veto + weighted ranking), payment ledger (PAY-2026-XXXXXX references, PaymentLedgerService, statements), booking FSM with actor authorization, notifications, driver route cockpit (trip status + /route re-plan + Google map), demo seeder (7 Sicilian personas, password demo123, only on empty DB, not in `test` profile), passenger-perspective search (yourRideMetrics in TripPlanningController — passenger sees own fare/time/distance, not the driver's totals), € currency everywhere, autocomplete, reputation card.

## Environment gotchas
- Sandbox has no Maven/JDK17 — user runs `mvn` in PowerShell and pastes output.
- Files sometimes appear truncated through the sandbox mount while the real file is fine (and vice versa) — always cross-check with the host-side file view before "repairing".
- Word locks the docx (`~$deNDrive_Report_v2.docx` lock file may linger; delete it or ignore).
- git: commits from sandbox work via lock-file workaround; push only from PowerShell.
- Maps key is env var GOOGLE_MAPS_API_KEY (setx affects only new terminals).

## Remaining checklist
1. User: re-run `mvn clean test` → expect **121 green**.
2. Close Word → copy fresh docx from session outputs into this folder (or ask Claude to redeploy).
3. Word: open docx → update fields → export PDF.
4. Commit + push everything from PowerShell.
5. Send REBUTTAL_EMAIL.md content to sdistefano@unime.it with report (docx+PDF) attached.
